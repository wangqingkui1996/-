require(["config"], function(){
	require(["jquery"],function($){
		var li=$(".carousel_li"),
		length = lis.length, 
		currentIndex = 0, 
		nextIndex = 1, 
		circles = $("i");
		console.log(li);
	});
});	
